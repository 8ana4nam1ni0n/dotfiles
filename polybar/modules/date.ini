[module/date]
type = internal/date
interval = 1

date = %b %d %H:%M
date-alt = %a, %b-%d-%y

format = <label>
format-prefix = 󰃰
format-prefix-foreground = ${colors.magenta}
format-prefix-padding = 1

label = %date%
label-foreground = ${colors.fg}
label-padding = 1
